---
title: React 常用模式
description: React 开发中常用的设计模式和最佳实践
---

## Compound Components

Compound components 是一种将相关组件组合在一起的模式。

使用 `React.createContext` 共享内部状态，让父组件控制子组件行为。

```tsx
function Tabs({ children }: { children: React.ReactNode }) {
  const [active, setActive] = useState(0);
  return (
    <TabsContext.Provider value={{ active, setActive }}>
      {children}
    </TabsContext.Provider>
  );
}
```

这种模式的优势在于 API 简洁、灵活度高。

## Render Props

Render Props 是另一种常见的组件复用模式，通过函数 prop 来共享逻辑。
